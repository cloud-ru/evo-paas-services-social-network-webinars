apiVersion: v1
kind: Secret
metadata:
  name: social-network-secrets
  namespace: social-network
  labels:
    app: social-network-webinars
type: Opaque
data:
  # PostgreSQL credentials (base64 encoded)
  POSTGRES_USER: cG9zdGdyZXM=  # postgres
  POSTGRES_PASSWORD: cGFzc3dvcmQ=  # password
  
  # JWT Secret (base64 encoded)
  JWT_SECRET: c3VwZXJzZWNyZXQ=  # supersecret
  
  # MinIO credentials (base64 encoded)
  MINIO_ROOT_USER: bWluaW8=  # minio
  MINIO_ROOT_PASSWORD: bWluaW8xMjM=  # minio123
  
  # SMTP Password (base64 encoded)
  SMTP_PASSWORD: cGFzc3dvcmQ=  # password
  
  # S3 credentials (base64 encoded)
  S3_ACCESS_KEY_ID: bWluaW8=  # minio
  S3_SECRET_ACCESS_KEY: bWluaW8xMjM=  # minio123